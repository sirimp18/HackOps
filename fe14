
Action: file_editor create /app/frontend/src/pages/Workers.jsx --file-text "import React from \"react\";
import { useApp } from \"../AppContext\";
import { Phone, Mail, Building2, Clock, MapPin } from \"lucide-react\";

export default function Workers() {
  const { workers, cities, city } = useApp();
  const cityObj = cities.find((c) => c.id === city);

  return (
    <div className=\"space-y-6\">
      <div>
        <h1 className=\"text-3xl font-bold\">Worker Contact</h1>
        <p className=\"text-slate-500 dark:text-slate-400\">
          {cityObj?.agency} — sanitation supervisors, drivers, and recycling partners.
        </p>
      </div>

      <div className=\"grid md:grid-cols-2 gap-4\">
        {workers.map((w) => (
          <div key={w.id} data-testid={`worker-${w.id}`}
               className=\"rounded-2xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-5 hover:border-emerald-400 transition\">
            <div className=\"flex items-start justify-between gap-3\">
              <div>
                <h3 className=\"font-bold text-lg\">{w.name}</h3>
                <p className=\"text-xs text-slate-500\">{w.role}</p>
              </div>
              <span className=\"text-[11px] bg-emerald-100 text-emerald-700 px-2 py-0.5 rounded-full font-bold\">
                {w.agency}
              </span>
            </div>
            <div className=\"text-sm text-slate-600 dark:text-slate-400 mt-4 space-y-1.5\">
              <div className=\"flex items-center gap-2\"><MapPin size={14} className=\"text-emerald-600\" /> {w.area}</div>
              <div className=\"flex items-center gap-2\"><Clock size={14} className=\"text-emerald-600\" /> {w.shift}</div>
              <a href={`tel:${w.phone}`} className=\"flex items-center gap-2 hover:text-emerald-700\">
                <Phone size={14} className=\"text-emerald-600\" /> {w.phone}
              </a>
              <a href={`mailto:${w.email}`} className=\"flex items-center gap-2 hover:text-emerald-700 break-all\">
                <Mail size={14} className=\"text-emerald-600\" /> {w.email}
              </a>
            </div>
            <div className=\"mt-4 flex gap-2\">
              <a href={`tel:${w.phone}`} data-testid={`call-${w.id}`}
                 className=\"flex-1 inline-flex items-center justify-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white py-2 rounded-lg text-sm font-medium\">
                <Phone size={14} /> Call
              </a>
              <a href={`mailto:${w.email}`} data-testid={`email-${w.id}`}
                 className=\"flex-1 inline-flex items-center justify-center gap-2 border border-emerald-600 text-emerald-700 hover:bg-emerald-50 dark:hover:bg-emerald-900/20 py-2 rounded-lg text-sm font-medium\">
                <Mail size={14} /> Email
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
"
Observation: Create successful: /app/frontend/src/pages/Workers.jsx