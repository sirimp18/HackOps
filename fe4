
Action: file_editor create /app/frontend/src/pages/Landing.jsx --file-text "import React from \"react\";
import { Link } from \"react-router-dom\";
import { ArrowRight, Trash2, Map, Sparkles, Bell, Users, Camera, CalendarDays } from \"lucide-react\";

const LOGO_URL = \"https://customer-assets.emergentagent.com/job_92b306da-9945-4d48-b2e7-0b5f34ddb981/artifacts/2k6kxcss_ChatGPT%20Image%20May%208%2C%202026%2C%2011_22_55%20AM%20%281%29.png\";

const FEATURES = [
  { icon: Map, title: \"Interactive Bin Map\",
    desc: \"Visualize every smart bin across Bengaluru and Mysore with color-coded fill levels and zone-aware insights.\" },
  { icon: Sparkles, title: \"AI Placement Recommendations\",
    desc: \"Get instant, data-driven recommendations for new bin placements based on foot traffic, complaints, and density.\" },
  { icon: Bell, title: \"Real-time Overflow Alerts\",
    desc: \"Bins crossing the red-fill threshold automatically generate dashboard alerts and notify the assigned worker.\" },
  { icon: CalendarDays, title: \"Event-Aware Planning\",
    desc: \"Plan additional bins around Dasara, IPL, marathons, and tech summits with attendee-driven projections.\" },
  { icon: Camera, title: \"Photo-based Complaints\",
    desc: \"Citizens can submit complaints with photos pinpointing zone, place, and area in seconds.\" },
  { icon: Users, title: \"Worker Coordination\",
    desc: \"BBMP and MCC contacts in one place — name, role, agency, phone, and email at a tap.\" },
];

export default function LandingPage() {
  return (
    <div className=\"min-h-screen bg-gradient-to-b from-emerald-50 via-white to-emerald-50 text-slate-900\">
      {/* Nav */}
      <nav className=\"px-6 md:px-12 py-5 flex items-center justify-between max-w-7xl mx-auto\">
        <div className=\"flex items-center gap-3\">
          <img src={LOGO_URL} alt=\"CitiBin\" className=\"w-12 h-12 rounded-xl object-contain bg-white shadow-sm\" />
          <div>
            <div className=\"font-extrabold text-2xl tracking-tight text-emerald-700\">CitiBin</div>
            <div className=\"text-[11px] text-slate-500 -mt-0.5\">Smart bins. Cleaner cities.</div>
          </div>
        </div>
        <Link
          to=\"/dashboard\"
          data-testid=\"launch-dashboard-btn\"
          className=\"hidden sm:inline-flex items-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white px-5 py-2.5 rounded-full font-medium shadow-md transition\"
        >
          Launch Dashboard <ArrowRight size={18} />
        </Link>
      </nav>

      {/* Hero */}
      <section className=\"px-6 md:px-12 max-w-7xl mx-auto pt-10 pb-20 grid md:grid-cols-2 gap-10 items-center\">
        <div>
          <span className=\"inline-block px-3 py-1 rounded-full bg-emerald-100 text-emerald-700 text-xs font-semibold mb-4\">
            BENGALURU · MYSORE · LIVE
          </span>
          <h1 className=\"text-4xl md:text-6xl font-extrabold tracking-tight leading-tight\">
            Smarter waste,<br />
            <span className=\"text-emerald-600\">cleaner streets.</span>
          </h1>
          <p className=\"mt-5 text-lg text-slate-600 max-w-lg\">
            CitiBin turns every public bin into a connected node — predicting overflows, routing workers,
            and helping cities respond before garbage piles up.
          </p>
          <div className=\"mt-8 flex gap-3\">
            <Link
              to=\"/dashboard\"
              data-testid=\"hero-launch-btn\"
              className=\"inline-flex items-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white px-6 py-3 rounded-full font-semibold shadow-lg transition\"
            >
              Launch Dashboard <ArrowRight size={18} />
            </Link>
            <Link
              to=\"/ai-recs\"
              data-testid=\"hero-ai-btn\"
              className=\"inline-flex items-center gap-2 bg-white text-emerald-700 px-6 py-3 rounded-full font-semibold border-2 border-emerald-600 hover:bg-emerald-50 transition\"
            >
              <Sparkles size={18} /> See AI Recs
            </Link>
          </div>
          <div className=\"mt-10 grid grid-cols-3 gap-4 max-w-md\">
            <Stat n=\"25\" label=\"Smart bins live\" />
            <Stat n=\"2\" label=\"Cities covered\" />
            <Stat n=\"11\" label=\"Worker contacts\" />
          </div>
        </div>
        <div className=\"relative\">
          <div className=\"absolute inset-0 bg-emerald-200/40 blur-3xl rounded-full\"></div>
          <img src={LOGO_URL} alt=\"CitiBin\"
               className=\"relative w-full max-w-md mx-auto rounded-3xl shadow-xl bg-white p-6\" />
        </div>
      </section>

      {/* Features */}
      <section className=\"px-6 md:px-12 max-w-7xl mx-auto pb-24\">
        <div className=\"text-center mb-12\">
          <h2 className=\"text-3xl md:text-4xl font-bold\">What's inside</h2>
          <p className=\"text-slate-600 mt-2\">Everything Bengaluru and Mysore need to run waste operations smarter.</p>
        </div>
        <div className=\"grid md:grid-cols-2 lg:grid-cols-3 gap-6\">
          {FEATURES.map((f, i) => (
            <div key={i} data-testid={`feature-${i}`}
                 className=\"rounded-2xl border border-slate-200 bg-white p-6 hover:border-emerald-300 hover:shadow-lg transition\">
              <div className=\"w-11 h-11 rounded-lg bg-emerald-100 text-emerald-700 flex items-center justify-center mb-4\">
                <f.icon size={22} />
              </div>
              <h3 className=\"font-bold text-lg\">{f.title}</h3>
              <p className=\"text-slate-600 mt-2 text-sm leading-relaxed\">{f.desc}</p>
            </div>
          ))}
        </div>
      </section>

      <footer className=\"border-t border-slate-200 px-6 md:px-12 py-8 text-center text-sm text-slate-500\">
        <Trash2 className=\"inline mr-2\" size={16} />
        CitiBin · Smart bins. Cleaner cities. Better tomorrow.
      </footer>
    </div>
  );
}

const Stat = ({ n, label }) => (
  <div>
    <div className=\"text-3xl font-extrabold text-emerald-700\">{n}</div>
    <div className=\"text-xs text-slate-500\">{label}</div>
  </div>
);
"
Observation: Create successful: /app/frontend/src/pages/Landing.jsx